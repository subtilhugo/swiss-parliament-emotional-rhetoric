# Copy this file to .env and fill in your key before running the labelling steps.
# The fetch step (00_fetch.py) does not require any key.

OPENAI_API_KEY=sk-...

# Optional: use a different model (default is gpt-4o-mini)
# OPENAI_MODEL=gpt-4o
